import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Transformation Specialist | Product-Led AI Consulting",
  description:
    "I help businesses adopt AI to ship faster, cut costs, and build real products. 5 production apps built solo with AI — I'll show your team how.",
  openGraph: {
    title: "AI Transformation Specialist",
    description:
      "Product Owner who builds entire SaaS products with AI. Teaching teams to do the same.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} antialiased`}
    >
      <body>{children}</body>
    </html>
  );
}
